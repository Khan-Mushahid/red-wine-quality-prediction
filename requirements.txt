streamlit==1.27.2
pandas==1.5.3
numpy==1.26.4
scikit-learn==1.2.2   # 👈 must match training
joblib==1.3.2
pyarrow==15.0.2
matplotlib==3.7.1      # Optional, if used in app (not needed if only in training)
seaborn==0.12.2        # Optional
